{
  "name": "subscription",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "db:push": "bunx drizzle-kit push --force --config=drizzle.config.ts",
    "db:pull": "bunx drizzle-kit pull --config=drizzle.config.ts",
    "db:seed": "bun run ./src/infra/database/seed/seeder.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
